#include <iostream>
#include <vector>
using namespace std;

class Solution{
public:
  int maxSubArray(vector<int>& nums){
    vector< vector<int> >subsum(nums.size(),vector<int>(nums.size(),0));

    for(int i=0;i<nums.size();i++){
      
    }
    return 0;
  }
};
