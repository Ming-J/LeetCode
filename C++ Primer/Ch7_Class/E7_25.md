Can `Screen` safely rely on the default versions of copy and assignment?

Yes, because all the data member type are build-in types or string. Which can rely on synthesized versions for copy and assignment.