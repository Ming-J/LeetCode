#include <iostream>
using namespace std;

class Solution{
public:
  int maximalSquare(vector< vector<char> >& matrix){
    return 0; 
  }

  void dsf(vector< vector<char> >& matrix, int r, int c,
	   int &top, int &left, int &bot, int &right){
    if(r>=0 && r < matrix.size() && c >= 0 && c < matrix[r].size()){
      if

    }
  }
  
}

  
